# Arrow Trade Docs

Standalone Astro + Starlight documentation site for the Arrow Trade research and launch strategy.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The build also runs `scripts/verify-docs.mjs` to confirm expected sidebar pages, Mermaid usage and internal links.
